# Agent LEARNINGS â€” Mistake Log & Correction Rules



*When an agent makes a mistake, it is logged here with the correction and the rule it generates.*

*Rules are active â€” they override default behaviour on the next cycle.*

*Updated by self_review.py and by Sage after each batch.*



---



## Active Rules (All Agents)



1. **Absence of data is not permission to guess.** If sharp odds are missing, verdict is UNKNOWN â€” not VALUE. If injury status is unclear, flag it â€” don't assume healthy.

2. **Thin markets invalidate edges.** A 12% edge on a $400 Polymarket market is not a 12% edge. Volume floor is $1,000 â€” below that, the price is noise.

3. **Do not confuse correlation with causation.** If a team wins 70% at home, that is a stat. It is not an edge unless Polymarket is pricing them below that rate.

4. **One data point is not a pattern.** Do not generate a rule from a single outcome. Minimum 3 observations before concluding something.

5. **Confirmed injuries open a 2% edge exception.** When Max confidence is HIGH and at least one high-impact injury is verified ("confirmed_out" via web_search or grok_twitter), edge >= 2% is sufficient to proceed (volume > $5,000 required). Markets price in verified news gradually — 2% is genuine value in that scenario. The 3% floor applies when injuries are unverified or Max confidence is medium/low.

6. **EPL via web search = skip. EPL on Polymarket = research.** The NO_MARKET issue was Max finding EPL games via web search that were NOT on Polymarket. EPL events that appear in the ACTIVE POLYMARKET SPORTS MARKETS list ARE valid research targets and MUST be researched — they have confirmed slugs and may have sharp odds. Only block EPL games discovered via web search speculation (not in the Polymarket list).
| 2026-02-28 | Max | All 5 events in this batch were EPL games that triggered research_timeout across every tool call, producing zero usable data (no form, no injuries, no H2H, no edge thesis). Max spent the full research budget on EPL despite repeated NO_MARKET outcomes and timeout failures in prior batches. | Max must not allocate research slots to EPL events unless a confirmed Polymarket slug is pre-verified from the active market list. When all tool calls return research_timeout for a candidate, Max should abort that candidate immediately and move to the next — not output a shell with zero data. | Never research an EPL event discovered via web search without a pre-confirmed Polymarket slug; abort any candidate where 3+ consecutive tool calls return research_timeout and reallocate that slot to a non-EPL sport. |
| 2026-02-28 | Lumi | All assessed events in this batch resulted in ABORT due to extremely low calculated edges (all < 1%), consistently falling below the 2% minimum threshold required for any scenario, and also below specific thresholds for injury/conflict scenarios. Nova also consistently flagged these edges as 'within noise margin, not enough to bet'. | Lumi should consistently ABORT bets when the numerical edge is below 2%, regardless of other factors, as this indicates no genuine edge is present according to the system's core rules. | Always ABORT if edge_pct < 2%. |
| 2026-02-28 | Nova | Across this batch and prior batches, NHL Polymarket markets are consistently returning edges of 0.1%–1.0% — all FAIR verdicts with zero VALUE signals. The batch history confirms NHL avg edge of 0.4% across 11 events with 0 bets placed. NHL markets on Polymarket appear to be as efficiently priced as NBA markets. | NHL should be deprioritized in Max's research queue unless there is a specific structural reason to believe a pricing gap exists (e.g., very large verified injury to a star player confirmed post-line-set). Spending research slots on NHL games that return sub-1% edges is wasteful. | Deprioritize NHL candidates unless Max has pre-identified a high-impact confirmed injury; NHL Polymarket edges have averaged 0.4% across 11 events — treat NHL as efficiently priced until evidence of a genuine gap emerges. |
| 2026-03-02 | Pipeline | Across this batch and 5+ prior batches, NBA Polymarket markets have produced sub-2% edges on every single event (30+ NBA events, avg edge well below 2%). The research budget continues to be consumed by NBA and NHL games that structurally cannot produce VALUE verdicts — Polymarket pricing is consistently tight with sharp books. | Max should pivot research allocation away from standard NBA and NHL moneyline events unless a confirmed high-impact verified injury creates a structural pricing gap pre-research. MMA, international soccer (non-EPL), and other untested markets must be prioritized in upcoming batches to find genuine Polymarket pricing gaps. | Never fill more than 3 of 8 research slots with NBA/NHL moneyline events unless Max has pre-identified a confirmed (not unverified) high-impact star player absence; prioritize MMA and non-EPL international soccer for remaining slots as these markets are structurally less efficient on Polymarket. |
| 2026-03-03 | Max | The `get_nba_game_log` API consistently timed out for all pre-fetched teams and subsequent calls, resulting in a lack of recent form data for NBA games. | When `get_nba_game_log` fails, immediately pivot to `web_search` for "[team name] last 5 games" to gather recent form data. | Always use `web_search` for NBA recent form if `get_nba_game_log` fails or times out. |
| 2026-03-03 | Lumi | Nova's `direction_conflict` field consistently reported `false` even when there was a clear conflict between Max's `max_verdict` and Nova's calculated `edge.side` across multiple events in this batch. This meant Lumi had to independently identify the conflict, which is a critical factor for applying the higher edge threshold in such scenarios. | Nova's `direction_conflict` logic needs to be revised to accurately reflect when Max's `max_verdict` and Nova's `edge.side` are contradictory. Lumi will continue to independently identify these conflicts. | Nova must explicitly flag `direction_conflict = true` when Max's `max_verdict` (e.g., HOME_ADVANTAGE) and Nova's `edge.side` (e.g., AWAY_EDGE) are contradictory. Lumi will treat any such observed contradiction as a `direction_conflict` and apply the required edge >= 8% threshold. |
| 2026-03-03 | Max | Across this batch and multiple prior batches, Max's edge_thesis direction (HOME_ADVANTAGE or AWAY_EDGE) conflicts with Nova's computed edge direction on 4 of 5 Lumi-assessed events. This is a recurring structural problem — Max is forming directional verdicts from injury narratives without anchoring to market-implied probabilities, producing direction conflicts that trigger the 8% edge threshold and guarantee ABORT. | Max must cross-reference his directional verdict against the available Polymarket price before finalizing max_verdict. If Polymarket already prices the injured team's opponent heavily (e.g., away team at 84.5%), Max's thesis must align with or explicitly explain the departure from that prior — not blindly assign AWAY_EDGE when the market already agrees with the injury narrative and has priced it in. | Always check the Polymarket implied probability before assigning max_verdict direction; if the market has already priced in the injury narrative (e.g., opponent at >70%), do not assign a directional verdict that conflicts with Nova's edge calculation — set max_verdict to UNCERTAIN instead of creating a false direction conflict. |
| 2026-03-03 | Pipeline | This batch produced 7 researched events with 0 VALUE verdicts from Nova — all NBA/NHL edges between 0.04% and 1.22%. Combined with the prior batch (0 VALUE, 0 bets), the pipeline has now gone 14+ consecutive batches with zero approved bets. NBA and NHL Polymarket pricing remains structurally efficient and is consuming the entire research budget with zero yield. | The research allocation must pivot immediately. Max must prioritize MMA and non-EPL international soccer in upcoming batches. NBA/NHL slots should be capped at 2 of 8 unless a pre-confirmed high-impact verified injury is identified before research begins. | Cap NBA+NHL combined research slots at 2 per batch until at least one VALUE verdict is observed from these leagues; fill remaining slots with MMA and international soccer (non-EPL) to test whether genuine Polymarket pricing gaps exist in untested markets. |
| 2026-03-03 | Lumi | Across this batch, all 5 events resulted in an ABORT due to calculated edges being consistently below the 2% minimum threshold. This was compounded by persistent discrepancies where Max's edge direction conflicted with Nova's calculated edge direction for all events, despite Nova's `direction_conflict` field reporting `false`. This required Lumi to manually identify these conflicts, triggering the 8% edge threshold which was also not met. | Nova's `direction_conflict` logic needs immediate revision to accurately flag directional disagreements. Furthermore, the pipeline must implement earlier filtering of events with sub-2% edges to conserve compute for Lumi. Lumi will continue to strictly enforce both the 2% absolute edge floor and the 8% edge threshold for observed directional conflicts. | Always ABORT if `edge_pct` is below 2% (absolute value). Always manually verify for `direction_conflict` between Max's `max_verdict` and Nova's `edge.side`; if a conflict is observed, the 8% edge threshold applies regardless of Nova's `direction_conflict` field. |
| 2026-03-03 | Pipeline | This is the 15th consecutive batch with 0 approved bets. All 10 events this batch are NBA/NHL with edges ranging 0.12%–1.99% — none approaching the 5% VALUE threshold. The pipeline continues to allocate 100% of research slots to NBA/NHL despite 60+ consecutive events confirming structural market efficiency on Polymarket. The directive to cap NBA/NHL at 2-3 slots and pivot to MMA/international soccer has not been implemented. | Max must immediately enforce the slot cap: no more than 2 NBA slots and 1 NHL slot per batch. Remaining 7 slots must go to MMA, non-EPL international soccer (La Liga, Bundesliga, Serie A, Ligue 1), or other untested sports with confirmed Polymarket markets. The current allocation is producing a 0% bet rate across 60+ researched events. | Never allocate more than 3 combined NBA+NHL slots per batch; fill all remaining slots with MMA or non-EPL soccer leagues that have confirmed Polymarket slugs — NBA/NHL Polymarket has shown 0 VALUE verdicts across 60+ events and must be treated as structurally efficient until proven otherwise. |
| 2026-03-03 | Max | Across this batch, 4 of 10 events show Max directional verdicts (AWAY_EDGE or HOME_ADVANTAGE) that conflict with Nova's computed edge direction: DAL/CHA (Max=AWAY_EDGE, Nova=HOME_EDGE), WAS/ORL (Max=AWAY_EDGE, Nova=HOME_EDGE), SAS/PHI (Max=HOME_ADVANTAGE, Nova=AWAY_EDGE), MEM/MIN (Max=AWAY_EDGE, Nova=HOME_EDGE). This is a persistent multi-batch pattern where Max assigns directional verdicts based on injury narratives without anchoring to Polymarket implied probabilities. | Before finalizing any directional verdict, Max must check the Polymarket price. If the market has already priced in the injury narrative (e.g., away team at 83.5% implies market agrees with AWAY_EDGE thesis), Max's thesis is redundant — no gap exists. When the market already reflects the injury narrative, Max should report UNCERTAIN rather than a directional verdict that will conflict with Nova's edge calculation. | Always cross-check directional verdict against Polymarket implied probability before outputting max_verdict; if the market already prices the favored side above 70%, the injury narrative is priced in — output UNCERTAIN rather than a directional verdict that creates a false direction conflict with Nova. |
| 2026-03-03 | Pipeline | All 3 events this batch are NBA games with edges of 0.01%, 0.44%, and 0.74% — continuing the structural pattern of zero VALUE verdicts from NBA Polymarket across 60+ consecutive events. The batch allocation was 100% NBA despite explicit rules capping NBA+NHL at 3 slots and mandating MMA/international soccer for remaining slots. | Max must enforce the slot cap immediately: no more than 2 NBA slots per batch. The remaining slots must go to MMA and non-EPL international soccer (La Liga, Bundesliga, Serie A, Ligue 1). This rule has been written 3+ times and ignored every batch. | Never allocate all research slots to NBA in a single batch; enforce the hard cap of 2 NBA slots maximum, and fill all remaining slots with MMA or non-EPL international soccer — NBA Polymarket has produced 0 VALUE verdicts across 63+ consecutive events and must be treated as structurally efficient. |
